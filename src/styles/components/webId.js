'use strict';

import variables from '../base/variables';
const { colors } = variables;

export default {
  info: {
    marginBottom: 30,
  },
  link: {
    color: colors.link,
    fontWeight: 'bold',
  },
};
