'use strict';

import variables from '../base/variables';

const { colors } = variables;

export default {
  text: {
    marginBottom: 15,
  },
  link: {
    color: colors.link,
    display: 'block',
    marginBottom: 15,
  },
};
