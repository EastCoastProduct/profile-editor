'use strict';

import variables from '../base/variables';
const { colors } = variables;

export default {
  link: {
    color: colors.link,
    display: 'block',
    marginBottom: 10,
  },
  linkIcon: {
    color: colors.font,
    marginRight: 10,
  },
};
